var webpack = require('webpack');

module.exports = {
    entry: './src/worley-noise.js',
    output: {
      libraryTarget: "umd",
      library: "WorleyNoise",
      path: './bin',
      filename: 'worley-noise.min.js'
    },
    plugins: [
        new webpack.optimize.UglifyJsPlugin({compress: {warnings: false}})
    ]
};
